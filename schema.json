{
  "title": "Parameters",
  "type": "object",
  "required": [
    "compress_output"
  ],
  "properties": {
    "compress_output": {
      "title": "Compress output (gzip)",
      "type": "boolean",
      "enum": [
        false,
        true
      ],
      "default": false
    }
  }
}